---
target: full-site
total_score: 21
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 3
timestamp: 2026-09-25T17-12-45Z
slug: full-site
---
Method: dual-agent (A: design review · B: detector + browser)

## Design Health Score (Persuade mode)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Submit success/failure is a transient toast only; form resets with no on-page confirmation |
| 2 | Match System / Real World | 3 | Phone placeholder `XXX-XX-XXXX` is the SSN pattern; "Tap to learn more" on desktop |
| 3 | User Control and Freedom | 3 | No fallback channel after a failed send |
| 4 | Consistency and Standards | 2 | Form industries (7) ≠ About industries (12); no Events option; two closing CTAs; ES `<title>` stays English |
| 5 | Error Prevention | 2 | All 5 fields required incl. phone + industry, no optional markers — contradicts "low-friction ask" |
| 6 | Recognition Rather Than Recall | 3 | Desktop Services carousel hides cards 4–5 (CRM) behind a fade |
| 7 | Flexibility and Efficiency | n/a | Single-page marketing surface |
| 8 | Aesthetic and Minimalist Design | 3 | Disciplined Register A; About overloaded (stats + timeline + marquee) |
| 9 | Error Recovery | 3 | Strong inline validation; network-failure toast has no alternative channel |
| 10 | Help and Documentation | n/a | Marketing landing page |
| **Total** | | **21/32** | **Acceptable (66%)** |

## Design Specificity Verdict
Authored in the hero (CFT_ cursor, numbered identity list) and Events (kiosk mockup); category-generic from About onward (stat trio, zig-zag process, industry marquee, filterable portfolio, centered form, second boxed CTA, 3-col footer). The cursor appears only in the logo and after the hero h1. In dark mode, Register A and Events share Night, so the office→event-floor shift is lost.
Detector: CLI 1 finding (overused-font, Space Grotesk — sanctioned by brief; CLI ran in degraded regex mode). Browser overlay: 99 findings on 81 elements; ~70 sanctioned Register B glow/mint (Events), 12 sr-only overflow false positives, 7 ScrollArea nested-card false positives. Real: em-dash overuse (16), a `transition: height`, Events context strip 4.48:1 (just under AA), hero/Events kicker labels (judgment call).

## Priority Issues
1. [P1] Event organizers have no conversion path — no CTA in Events, no Events option in form, Experiential line hidden above the fold on mobile. Fix: Events CTA deep-linking to #contact with a pre-selected "Event / booth activation" topic; compact identity row on mobile. → clarify, layout
2. [P1] Contact form friction at the commit moment — phone + industry required, SSN-shaped placeholder, toast-only success, no mailto fallback. Fix: optional phone/industry with markers, `787-555-0123`, inline success panel, mailto in error state. → harden
3. [P1] Two competing closing CTAs (form, then Calendly card); no persistent nav CTA. Fix: one primary; Calendly as secondary link beside Send; add "Start a project" to navbar + mobile menu. → distill
4. [P2] Brand signature stops after the hero; About→Footer is template-generic. Fix: cursor as recurring marker (active nav, success state, footer sign-off); replace stat trio with named, verifiable outcomes. → bolder, typeset
5. [P2] Dark-mode register collapse + Services carousel hides content on desktop. Fix: tint Register A surfaces in dark or deepen Events; grid Services at ≥lg. → layout, colorize

## Persona Red Flags
- Jordan: can't tell which of two CTAs is the real next step; "10+ Satisfied Clients" with no names/logos.
- Riley: empty submit → five stacked errors; phone regex rejects extensions; ES title/meta English; ES Events h2 wraps to 4 lines on mobile.
- Casey: ~9,360px page; mobile menu has no CTA and no scrim; Experiential line hidden.
- Marisol (event organizer): excited by kiosk, then no proof (no event photos/case study), no CTA, form can't capture event date/booth.

## Minor Observations
- Events context strip rgba(255,255,255,.45) mono 12px = 4.48:1 (below AA).
- 16 em-dashes in body copy; one `transition: height` somewhere (layout thrash).
- About stat labels ~11px, low emphasis.
- Work cards' saturated client colors fight the restrained palette; "Coming soon" tile undercuts credibility.
- "SaaS Platforms" is a stretch for the SMB audience.

## Questions to Consider
- If the kiosk is the most memorable thing on the page, should Events have its own page or `?for=events` variant with its own proof and CTA?
- Do you need both a form and Calendly?
- What would About look like with zero numbers and three named clients?
